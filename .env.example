# Optional overrides for docker-compose.yml — copy to .env and edit if you want
# non-default database credentials. Defaults (mgh / mgh_dev_password / mgh_travel)
# work fine for local/demo use without this file.
POSTGRES_DB=mgh_travel
POSTGRES_USER=mgh
POSTGRES_PASSWORD=mgh_dev_password
